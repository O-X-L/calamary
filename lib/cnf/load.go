package cnf

/*
import (
	"gopkg.in/yaml.v3"
)

func Load() {
	// todo: transform value fields to list if they are not (ips, ports, ...)
	yamlData, err := yaml.Marshal(data)
	err = yaml.Unmarshal(yamlData, &CONFIG)
}
*/
